Add a dedicated Home Assistant HTTPS webhook preset that sends the complete native
NDJSON feed, persists the webhook ID in Keychain, supports scoped exports and
portable credential-free configuration, and states that webhook delivery does not
itself import historical sensor states.

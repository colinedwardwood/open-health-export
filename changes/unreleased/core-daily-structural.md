Add sleep, mindful sessions, and workouts to the selectable picker and Core
Daily preset. Quantity catalogue (and Home Assistant discovery) stays on the
curated quantity list; category and workout families are a separate selectable
set so first-run export can include them without inventing HA sensors.
Authorization still requests only the enabled set, never the full catalogue.

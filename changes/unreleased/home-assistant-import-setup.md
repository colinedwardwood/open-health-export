Home Assistant webhook configuration imports now load their credential-free base URL,
mode, insecure-transport choice, and scope into the dedicated setup path. The webhook
ID remains empty and the imported draft cannot enable without a new real-path test.

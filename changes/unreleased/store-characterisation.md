Add an opt-in store-characterisation tool (`swift run characterise`) that
emits only aggregate shape statistics. A taint corpus of canary values,
source names, and UUIDs must not survive the JSON (QA-08). Real volunteer
stores are still unrecorded.

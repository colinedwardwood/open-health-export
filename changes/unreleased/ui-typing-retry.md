Make the UI-test typing retry aim at the field's own coordinate space after an
element tap fails to take keyboard focus, and say what the field looked like
when it never did. Three identical taps were one attempt, not three.

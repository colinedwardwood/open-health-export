History records outcome, trigger, window, per-type counts, bytes, duration,
per-step timings, and a redacted payload with an on-disk reveal path. The list
stays problems-first and names the 90-day / 1,000-run retention bound.

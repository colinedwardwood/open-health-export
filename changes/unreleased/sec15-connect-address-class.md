SEC-15: re-resolve local/private destinations immediately before every HTTP
or byte-stream connection. Plaintext and stream connectors bind the checked
numeric address; public, mixed, or unclassified answers fail closed, while
normal public HTTPS remains unrestricted.

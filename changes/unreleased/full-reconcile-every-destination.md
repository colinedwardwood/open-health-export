---
category: fixed
---

Run the full-history reconcile against every enabled destination. Both the
manual sweep and the daily scheduled one repaired only the archive folder, so
an HTTPS, Home Assistant, MQTT, or Mac companion destination never received the
records a delta cannot see. Each destination is reconciled against its own
granted scope, and a destination with no transport in the current context keeps
its queued obligations instead.

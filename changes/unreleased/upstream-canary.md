Nightly QA-22 canaries compare Home Assistant and Mosquitto stables to the
declared pins, exercise the real latest release containers, and open one
deduplicated tracking issue per divergence. They are schedule and dispatch
only, never required pull-request checks.

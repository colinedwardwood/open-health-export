QA-14 destination status and export history now show the closed error-class
sentence next to the reason code, so a failed send names what happened instead
of only a token like destinationUnreachable.

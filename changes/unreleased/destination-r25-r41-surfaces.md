The iOS harness now enables the local archive through `DestinationSetup`:
canary preview, pin-without-TLS, real-path write/read/confirm, then enable.
A stored passing test report resumes without re-emitting R-40 events.
Trust events increment the widget's unacknowledged change count and post
notices. Launch verifies ledger integrity; destination changes can be
acknowledged in place. Two-tap controls stop heart-rate export and run a
destructive wipe. `Where your data goes` and the README document Apple's
Hidden Apps recovery path. `policycheck` rejects `CFBundleAlternateIcons`.

Show freshness target N per class A–D, each pending R-71.

R-24 requires a per-class number before the user relies on export freshness.
Until soak evidence exists, the in-app lines and the README say so instead of
inventing a global N.

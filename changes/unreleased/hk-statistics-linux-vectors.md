Linux CI now replays a committed synthetic pipeline vector for every
hkStatistics exception metric. The vectors are not Apple capture evidence;
canonical numerical correctness remains the R-87 device pass.

RTL software keyboards no longer have to expose identifier `return`. Destination
status lines wrap and keep a 44-point hit target, and destination-change / demo
confirm / confirmation copy wrap instead of clipping under doubled strings.

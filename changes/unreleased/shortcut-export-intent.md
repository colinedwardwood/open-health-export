Add a Shortcuts action that runs one export page.

R-68 needs an App Intent, not only a status query. `Export one page` refuses
until disclosure and the local archive are enabled, then records
`RunTrigger.shortcut` in the journal.

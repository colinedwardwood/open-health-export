Focused coverage on push no longer expands an empty bash array under
`set -u`. Darwin host tzdata pins list both GitHub-hosted macos-26 (`2025b`)
and current developer Darwin (`2026c`) so R-84 still fails on a third
release.

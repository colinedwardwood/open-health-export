Name HTTPS, MQTT, and companion export progress against a known type total.

Those runs already iterate a scoped metric list, so UX-23 requires a determinate
Reading N of M types line instead of an unnamed Working spinner.

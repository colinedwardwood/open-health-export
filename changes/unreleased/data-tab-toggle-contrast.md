Data-tab date-limit and filter switches hide the system Toggle title and
the visible caption from VoiceOver, leaving one labeled switch. Hosted
Xcode 26.6 still scored the caption as unlabeled contrast after `.combine`.

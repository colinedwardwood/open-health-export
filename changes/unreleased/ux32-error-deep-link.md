Open the five-part error from a failure notification or a Status row within two taps.

A failure notification tap reconstructs `openhealthexporter://error` with the
destination and archetype. A Status attention row is the remaining tap. The
timed five-person study stays an external gate.

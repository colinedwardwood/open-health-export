Hosted iPad Dynamic Type audits on Xcode 26.6 fail captions and control
titles that use `fixedSize(vertical: true)` (Latest, Load Health, demo
honesty, MQTT). Wrap with line limits instead, and use one scalable
body font for the demo honesty line.

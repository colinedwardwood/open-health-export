### Seeded P6/P10 property invariants

Runs 4,000 deterministic mutation steps proving delta/full receiver convergence and 500 generated
folds proving cumulative-sum and discrete-mean conservation. A host process is also killed at every
ExportRun seam; a fresh store and runner must recover exactly one delivered sample.

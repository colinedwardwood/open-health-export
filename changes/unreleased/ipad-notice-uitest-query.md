The iPad-only exporter notice UITest matches the combined accessibility
element by identifier, not by Other type, so the iPad simulator pass
finds the same control the iPhone force-flag pass already did.

HTTPS and MQTT enablement now probes first, then waits for an explicit
"This is my server" confirmation of the grouped SPKI and dry-run canary.
Unacknowledged destination changes keep a non-dismissible in-app banner, and a
denied notification on those events is recorded as a security ledger entry.

Every push and fork PR runs gitleaks over git history with a checksum-pinned binary.
SECURITY.md now has the QA-06 steps for leaked health data or secrets. macos-build
gets a 20-minute budget so the RTL/pseudo-locale UI audit can finish.

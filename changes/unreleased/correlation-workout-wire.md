### Correlation and workout structural records

Adds canonical blood-pressure correlation containers with standalone component quantities, plus
workout records that preserve HealthKit duration, raw activity type, named total statistics,
events, route-presence metadata, and included-series declarations. Both kinds participate in
pipeline census/indexing and receiver upsert convergence.

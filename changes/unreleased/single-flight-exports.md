Two exports of the same metric no longer overlap. A Shortcut, the Control Centre
control, an observer wake or a background task that starts while that metric is
already exporting waits and then continues from where the first run left off, so
a page is never delivered twice (#31).

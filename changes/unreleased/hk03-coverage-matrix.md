HK-03 now fails CI when a HealthKit header identifier is neither in the
selectable catalogue nor listed with a reason. Darwin compares the live SDK
header to the committed pin and checks quantity aggregation styles.

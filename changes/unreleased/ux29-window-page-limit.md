Scale HealthKit pages with the owned export window so a 413 shorten-window fix reduces the next batch.

Older undelivered samples stay in the queue; only the page the destination refused gets smaller.

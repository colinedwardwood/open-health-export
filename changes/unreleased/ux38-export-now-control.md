Control Centre now offers an Export now control that opens the app on
`openhealthexporter://export-now` and runs one page under the widgetControl
trigger. Shortcuts return a typed outcome (success, Partial, failed, deferred,
blocked) and can export one type through the owned window.

Overwrite DEBUG imported-draft seeds instead of skipping when a leftover
draft exists, so later XCUITest cases in the same simulator see the kind
they asked for. Identify the Destinations ledger-honesty line.

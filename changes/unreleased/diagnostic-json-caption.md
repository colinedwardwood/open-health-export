---
category: fixed
---

The diagnostic JSON preview is no longer one 600-point label: each line is its
own wrapping row, VoiceOver hears a one-line caption, and the identifier the
audit measures is that caption. Empty history rows allow wrapping so Dynamic
Type can grow the line instead of reporting the font as unsupported.

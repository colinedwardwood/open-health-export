Status's time-to-first-screen line now uses the wrapping primary caption instead
of footnote, so the all-destination accessibility audit can scale it. Contrast
findings in presented Settings that sit in the sheet's bottom fade are classified
with the existing tab-bar suppression, matching the tall top fade already used
for Settings navigation chrome.

The release demo export now emits every supported Native Wire record family and
every selectable HealthKit characteristic without requiring HealthKit history,
rather than limiting the synthetic export to quantity metrics.

Carry opt-in W3C traceparent on the Mac companion offer as an explicit
protocol field, advertised by capability so older receivers never see
trailing bytes. The Mac strips inbound values and starts its own root.

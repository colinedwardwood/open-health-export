# FIX catalogue and R-80 adjacency

The committed FIX-* catalogue is enumerated by a meta-test. Inverted sample
intervals are rejected at encode time. Local Network denial has distinct copy
from an unreachable Mac. `policycheck` diffs `swift package dump-package`
against `spec/v1.0.0/adjacency.json`.

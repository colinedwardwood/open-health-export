Seeded five-part error fixtures are applied in onAppear so Status shows them
on first paint. Waiting until after queue and pairing work left the UITest
scrolling an empty Status page, then hunting past lines that appeared at the
top.

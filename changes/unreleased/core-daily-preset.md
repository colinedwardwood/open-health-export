Pin the first-run Core Daily preset to named routine types instead of
every routine metric the catalogue will ever grow. Sensitive types stay
out (R-66). Blood glucose stays selectable, not pre-selected (R-61).

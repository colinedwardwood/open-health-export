The T2 volume checker now counts synthetic `tombstone` records as accounted
structural input and uses 10,000-record pages so the concentrated heart-rate
prefix can finish inside the weekly job budget. A two-record slice keeps the
tombstone path on Linux CI.

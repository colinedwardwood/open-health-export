Linux gzip now imports the `CZlib` system module (same pattern as sqlite) so
`import zlib` is not required on Ubuntu. MQTTS presents a PKCS#12 client identity
and Mosquitto contracts cover a real test CA plus required client certificates.

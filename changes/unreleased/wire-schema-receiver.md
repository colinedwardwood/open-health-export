### R-12 schema and receiver gates

Adds the `ohe.wire/1` JSON Schema, structural freeze-diff classification, corpus validation,
forward-compatible receiver conformance fixtures, and the one-command `receiver` executable.

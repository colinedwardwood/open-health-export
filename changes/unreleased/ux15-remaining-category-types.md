Remaining HealthKit category types join the picker. Bloating maps to its own
identifier and abdominal cramps is separate. EnvironmentalAudioExposureEvent
stays excluded because this SDK returns no HKCategoryType for that pin.

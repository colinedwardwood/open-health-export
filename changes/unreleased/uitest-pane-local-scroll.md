UITests that only live on Data, Status, History, or Settings open that
pane once and scroll it, matching the Destinations helpers already on main.
Settings is also a Status-page control, so tests do not depend on the
navigation-bar gear.

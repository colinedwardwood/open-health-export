### First non-quantity structural family

Adds interval category records, canonical `sample.category` encoding and schema validation,
upsert/tombstone receiver convergence, census/index participation, and a HealthKit sleep-category
adapter that preserves evening-to-morning measurement spans and source provenance.

Give the privacy lock screen a full-window background and let its copy take
the ideal height, so the accessibility text-clip audit stops reporting the
lock title and background-scope sentence as clipped.

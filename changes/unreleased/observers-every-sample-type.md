Selecting sleep, workouts, mindful sessions or another non-quantity type no longer
switches off background Health delivery for every metric. Each type registers its
own observer; a type that cannot register is named on Status, and a wake still runs
its export pass (#28).

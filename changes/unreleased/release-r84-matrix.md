Release validation now requires every R-84 determinism matrix leg—Linux x86_64,
Linux ARM64, and macOS—to succeed. One green matrix job can no longer satisfy the
release gate while another platform fails.

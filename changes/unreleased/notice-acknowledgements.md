OSS-18: generate NOTICE from Package.swift and render it in Acknowledgements.

policycheck fails if NOTICE is stale. The iOS harness and Mac companion load the same
file from the app bundle. Destination and diagnostic controls now declare a 44 pt
minimum hit target so the empty-destination accessibility audit can cover that form.

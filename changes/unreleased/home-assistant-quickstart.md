The primary Home Assistant webhook destination now has a repository quickstart
covering automation setup, app scope and destination confirmation, end-to-end
verification, the plain-HTTP warning, designated-exporter selection, and the
important limitation that a webhook does not itself import historical states.

The README links the quickstart. Its required clean-install pass by a
non-maintainer remains external release evidence and is not claimed by CI.

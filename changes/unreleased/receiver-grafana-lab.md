Ship an in-repo R-115 lab stack: `receiver serve` exposes Prometheus metrics
and NDJSON ingest, and `receiver/compose.yaml` provisions Grafana with a
Classic schemaVersion 36 dashboard. Catalogue upload stays external.
